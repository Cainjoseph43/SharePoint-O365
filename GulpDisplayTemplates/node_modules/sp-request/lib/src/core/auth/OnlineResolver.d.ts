import { OptionsWithUrl } from 'request';
import * as Promise from 'bluebird';
import { IAuthResolver } from './IAuthResolver';
import { IAuthOptions } from './IAuthOptions';
export declare class OnlineResolver implements IAuthResolver {
    private static _cookieCache;
    applyAuthHeaders(authOptions: IAuthOptions): Promise<OptionsWithUrl>;
    private setHeaders(options, cookie);
}
